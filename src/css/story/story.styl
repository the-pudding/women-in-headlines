/* CUSTOM STORY STYLE */
/* CSS Styles */

.txtscroll {
    height: 3rem;  
    background-color: $red;
    display: flex;
    align-items: center;
}

.txtscroll p {
  font-size: 1.25em;
  color: $off-black;
  font-family: $barlow;
  text-transform: uppercase;
  font-weight: 300;
  font-style: italic;
}

/* ---------------------- */
body {
    background-color: $bg;
    overflow-x: hidden;
}

.annotation {
    fill: $off-black;
    font-family: $barlow;
    font-weight: 600;
    font-size: 14px;
    text-anchor: middle;
    transform: translate(-45px, 6px)
}

.button-wrapper {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
}

.annotationButton {
    fill: $off-black;
    font-family: $barlow;
    font-weight: 400;
    font-size: 14px;
    margin: 0;
    text-align: left;
}

.bubbleFilters .annotationButton {
  width: 3rem;
}

.padded-dropcap {
    padding: 1rem 1.25rem 1rem 1rem;
    float: left;
    position: relative;
    top: -0.1em;
    margin: 1rem 1.25rem -0.5rem 0.25rem;
    font-size: 64px;
    font-family: $barlow;
    color: $off-black;
    font-weight: 900;
    background-color: $pink;
    border 1px solid $off-black;
    transform: skew(-10deg, 0deg);
    line-height: 0.8;
  }

  #hoverGuide {
    font-size: 16px;
    fill: $off-black;
    font-weight: 600; 
    font-family: $barlow;
  }

  #hoverGuideLine {
      stroke: $off-black;
      stroke-width: 0.8px;
  }

  .txt-rotate {
    fill: $off-black;
  }
  
#tooltipText {
    font-family: $barlow;
    display: block;
    text-transform: uppercase;
    
}

.tooltipTL {
    font-family: $barlow;
    color: $off-black;
    background-color: $tan-darker;
    border: 1px $off-black solid;
    border-top: 2px $off-black solid;
    margin: 1rem;
    padding: 1rem;
    width: 15rem;
    max-height: 12rem;
}

.tooltipTemp {
    font-family: $barlow;
    color: $off-black;
    background-color: $bg;
    border: 1px $off-black solid;
    border-top: 2px $off-black solid;
    margin: 1rem;
    padding: 1rem;
    width: 16%;
    // max-height: 15rem;
    margin-left: 82%
}

.datettip {
    color:$red;
    text-transform: uppercase;
    font-size: 16px;
    font-family: $barlow;
    font-weight: 600;
}

.exampleheadlines {
    display: flex;
    flex-wrap: wrap;
    width: 100%;
    flex-direction: row;
    justify-content: center;
    padding: 0 1rem;
    margin 0 auto;
}

.exampleheadlines .btn-wrapper {
  width: 100%;
  display: flex;
  justify-content: center;
}

.exampleheadlines button {
  font-family: $barlow;
  font-size: 16px;
  font-weight: 600;
  width: 8rem;
  padding: 0.25rem;
  margin: 0.25rem;
  border-radius: 0.25rem;
  border: 2px solid $off-black;
  display: flex;
  justify-content: center;
  align-items: center;
}

.exampleheadlines button:hover {
  background: $off-black;
  color: $tan;

  svg {
    stroke: $tan;
  }
}

.exampleheadlines button svg {
  stroke: $off-black;
  height: 16px;
  width: 16px;
  margin-left: 0.25rem;
}

.tooltip {
    top: 10px;
    left: 0px;
    padding: 1rem;
    color: $off-black;
    background-color: $tan-darker;
    border: 1px $off-black solid;
    border-top: 2px $off-black solid;
    position:fixed;
    opacity: 0;
    z-index:1000;
    transition: opacity 0.25s ease-in;
    width: 15rem;
    visibility: hidden;
}

.is-visible {
  opacity: 1;
  visibility: visible;
}

.forceLogo {
  pointer-events: none;
}

.tt-date, .tt-site, .tt-hed, .tt-hed-no-italic {
  margin: 0;
}

.tt-date, .tt-site {
    font-family: 'Barlow Condensed';
    color: $off-black;
    font-size: 14px;
    font-weight: 600;
    margin: 0;
    text-transform: uppercase;
}

.tt-hed {
    font-family: 'Barlow Condensed';
    font-weight: 300;
    font-size: 16px;
    font-style: italic;
}

.tt-hed-no-italic {
    font-family: 'Barlow Condensed';
    font-weight: 300;
    font-size: 16px;
}

#tooltipHeadline path {
    fill: none;
}

.forceText {
  font-weight: bold;
  font-family: $national;
  font-size: 8px;
  text-anchor: middle;
  fill: silver;
}

.forceCircles {
    fill: $bg;
    stroke: $off-black;
    stroke-width: 1px;
    cursor: pointer;
}

.themesText {
    font-weight: 600;
    font-family: $barlow;
    font-size: 14px;
    text-anchor: middle;
    fill: $off-black;
    text-shadow: -1px -1px 0 $tan, 1px -1px 0 $tan, -1px 1px 0 $tan, 1px 1px 0 $tan;
    }

.xAxis line, .yAxis line{
  stroke: silver;
}

.xAxis text {
  font-family: $national;
}

.xAxis path, .yAxis path {
  display: none;
}

#xAxisLabel {
    font-size: 14px;
    fill: $off-black;
    font-family: $barlow;
    font-weight: 600;
    margin: 1rem 0 0 0;
}

.axisLabel {
  font-weight: bold;
  font-family: $national;
}

h1 {
    font-family: Georgia;
    font-style: bold;
    color: $off-black;
    }

.title {
    font-weight:900;
    font-size:130px;
    text-align: left;
    font-family: $barlow;
    text-transform: uppercase;
    color: $off-black;
    line-height: 1;
}

.title span {
    font-family: $barlow;
    font-weight:900;
    border-bottom: 1px solid $off-black;
    display: block;
    padding: 1rem 2rem 0rem 2rem;
    font-style: italic;
    line-height: 0.8;
}

.title span:last-of-type {
    border-bottom: none;
}

.subTitleArt {
    font-weight:300;
    font-size: 24px;
    font-family: $barlow;
    text-transform: uppercase;
}

.creditLink{
    color: $off-black;
    text-transform: uppercase;
    font-weight: 600;
}

.credits {
    font-weight:400;
    font-size:16px;
    font-family: $barlow;
    color: $off-black;
    max-width: 10rem;
}

.sectionHeader {
  font-family: $barlow;
  font-style: italic;
  font-weight: 900;
  color: $off-black;
  text-align: center;
  width: 100%;
  font-size: 48px;
  margin: 6rem auto 0.5rem auto;
  text-transform: uppercase;
  line-height: 0.8;
  border-bottom: 1px solid $off-black;
}

h2 {
    font-family: Georgia;
    font-style: bold;
    color: $off-black;
    }

h4 {
  font-family: $tiempos;
  font-size: x-large;
  font-style: bold;
  color: $off-black
}

h5 {
  font-family: $tiempos;
  font-size:medium;
  font-weight: 600;
  text-align: center;
  margin-bottom: 0;
  color: $off-black;
}

.description {
  max-width: $column-width;
  color: $off-black;
  font-family: $tiempos;
  margin: 0 auto;
  padding: 1rem 1rem;
  text-align: left;
}

ul {
  max-width: $column-width;
  margin: 0 auto;
  padding-left: 1.5rem;
  list-style-type: disc;
}

.intro-questions {
  margin: 0 auto;
  padding: 1rem 0;
  font-family: $tiempos;
  font-style: italic;
}

.stackedChartDescription {
    max-width: 24rem;
    text-align: left;
    color: $off-black;
    font-family: $barlow;
    font-size: 18px;
    line-height: 1.75;
}

.scrolltext {
  padding: 1rem 1rem 0 1rem;
  font-family: 'Tiempos Text Web';
  font-size: 18px;
  line-height: 1.75;
  text-align: left;
  margin-bottom: 0;
}

.deets {
  font-family: $barlow;
  color: $off-black;
  font-size: 14px;
  font-weight: 600;
  margin: 0;
  text-transform: uppercase
}

// .exampleheadlines .headline {
//   font-family: $barlow;
//   font-weight: 300;
//   font-size: 18px;
//   font-style: italic;
//   text-transform: uppercase;
// }

.headline {
  font-family: $barlow;
  font-weight: 300;
  font-size: 16px;
  font-style: italic;
  margin: 1rem 0 0.5rem 0;
}

#stackedLegend {
  display:flex;
  flex-direction: column;
  max-width: 210px;
  max-height: 30px;
  margin-top:20px;
  align-items: center
}

#stackedLegendText {
  // font-size:10px;
  max-width: 200px;
  font-family: $barlow;
  font-size: 14px;
  font-weight: 600;
  }

#stackedLegendArrow {
  font-size:20px;
  max-width: 10px;
  margin-left:0.2rem;
  margin-right:0.5rem
  }

#stackedLegendrect {
  fill:#ccc;
  }

#stackedLegendsvg {
  max-width: 58px
  }
   

.subtitle {
  margin-top: 0;
  margin-bottom: 10;
  display: flex;
  justify-content: center;
}

.header {
  justify-content: center;
  display: flex;
  text-anchor: middle;
}

.titleArt {
  display: flex;
  flex-direction: row;
  margin-top: 2rem;
  height: 508px;
  margin-bottom: 5rem;
  border-bottom: 1px solid $off-black;
}

.titlewords {
  width: 70%;
  border-right: 1px solid $off-black;
}

.titlesep {
  width: 100%;
  border: 0.1px solid $off-black;
  transform: translate(0,32px);
  line-height: 0px;
  margin-top: 0px;
  margin-bottom: 0px;
}

.titleCredits {
  width: calc(30% - 3rem);
  padding: 10rem 1.5rem;
  display: flex;
  flex-direction: column;
  justify-content: center;
  min-width: 18rem;
}

.dropOptions {
  font-family: $tiempos;
  font-size: x-large;
  font-weight: bold;
}

.buttons {
  justify-content: center;
  align-items: center;
  display: flex;
  width: 100%;
  display: flex;
  flex-direction: row;
  padding: 0.5rem 1rem 0rem 1rem;
  background-color: $tan;
  //position: sticky;
  //top: 0;
  //border-bottom: 1px solid $off-black;
}

button {
  border-color: $bg;
  background-color: $bg;
  color: $off-black;
  font-weight: 400px;
  border-style:solid;
  border-width: 1;
  padding: 0px 20px;
  margin-top: 5;
  text-align: center;
  text-decoration: none;
  font-size: 20px;
  font-family: $national;
  text-transform: uppercase;
}

#wordFreq {
    margin-top: 0;
    margin-bottom: 100
}

button:hover {
  background-color: $bg;
  color: $off-black;
}

select::selection{
    background-color: #F7DC5B;
    color: $off-black;
    font-weight: 600;
}

.country-active {
    background-color: #F7DC5B;
    color: $off-black;
    font-weight: 600;
    pointer-events: none;
}

.flag {
    height:30px;
    text-anchor: middle;
    padding-right: 0.5rem;
    max-width: 30%;
}

.country {
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
    border: 1px solid $off-black;
    border-radius: 0.25rem;
    width: 10rem;
    margin: 0.5rem;
    padding: 0;
    height: 2rem;
}

.country p {
  margin: 0;
  padding: 0;
  font-family: $barlow;
  font-size: 18px;
  // font-weight: 400;
}

.search {
  text-align: center;
}

.legend circle {
  fill: none;
  stroke: $off-black;
}

.legendBubble {
  fill: none;
  stroke: $off-black;
  stroke-width: 0.8;
}

.legend text {
  fill: silver;
  font: 8px sans-serif;
  text-anchor: end;
}

.svg-container {
  width: 100%;
  max-width: 60rem;
  padding: 0 1rem;
  height: 60vh;
}

.svg-content {
  display: inline-block;
  position: absolute;
  text-align: center;
  top: 50;
  left: 0;
}

#tooltipCluster {
    border: solid 1px $off-black;
    padding: 10px;
    /* change the tooltip so that its position can be moved around */
    position: fixed;
    /* hide it for when theres no hovering */
    display: block;
    /* background-color: white; */
    color:$off-black;            
}

#tooltipInfo {
  border: solid 1px $off-black;
  padding: 10px;
  max-width: 180px;
  text-align: justify;
  /* change the tooltip so that its position can be moved around */
  position: fixed;
  /* hide it for when theres no hovering */
  display: block;
  color: $off-black; 
  background-color: $bg;           
}

.clusterLegend {
  display: block;
  margin-left: auto;
  margin-right: auto;
}

.netbars {
  display: grid;
  grid-template-columns: 27vw 65vw; 
  grid-template-rows: 60vh 60vh 60vh 60vh;
  column-gap: 0vw;
  row-gap: 40vh;
  align-items: center;
  margin-top:30vh;
  margin-left:4vw;
  margin-right:4vw;
}

.barLegend {
  margin-top:20px;
  padding-bottom:10px
}

.barLegendText {
  font-size: 13px;
  font-family: $tiempos;
  fill:silver;
  text-anchor: start;
  text-align:left;
  opacity:0.9;
  font-weight: 800;
}

.hover-icon {
  font-weight: 600;
  font-family: $tiempos;
}

.hover-icon::before {
  content: '';
  background-image: url("./assets/images/touch.png");
  background-size: 24px;
  width: 24px;
  height: 24px;
  display: inline-block;
  position: relative;
  top: 0.3rem;
  left: 0rem;
	margin-right: 0.25rem;
}

/* sticky axis for wavy chart */
#stickyXaxis {
  position: sticky;
  top: 90px;
  background-color: $tan;
  fill: $off-black;
  color: $off-black;
  stroke-width: 1;
  font-weight: 800;
  font-size: 12;
  border-bottom: 1px solid $off-black;
  padding: 0rem 1rem 1rem 1rem;
  height: 150px;
}

#stickyXaxis p {
  font-family: $barlow;
  font-size: 14px;
  font-weight: 600;
  margin: 0;
  position: absolute;
  top: 3.85rem;
  left: 12%;
}

.stickyAxis .tick text {
  font-family: $barlow;
  font-size: 14px;
  font-weight: 400;
}

.axisThemeLegend {
  transform: translate(-25px, 85px);
  display: flex;
  flex-direction: row;
  text-align: center;
  justify-content: center;
  // max-width: 100px;
  // margin-left: 5%;
  // width: 500px
}

.wordText {
  text-anchor: end;
  fill: $off-black;
  font-weight: 600;
  font-size: 14px;
  font-family: $barlow;
  text-transform: uppercase;
  cursor: pointer;
}

.SMCell {
  cursor: pointer;
}


.timeRuler {
  z-index:10;
  position:fixed;
  stroke:$off-black;
  fill: none;
  stroke-width: 2;
  width: 20;
}

/* FIRST BAR CHART */

.container {
  display: flex;
  flex-wrap: nowrap;
  flex-direction:row;
  justify-content: center;
}

.freqBars {
  width: 16vw;
  height:35vw
}

.movingHeadline{
  font-size: 20px;
  font-family: sans-serif
}

#hlAnimation {
    height: 15vh;
}

.barChart {
  text-anchor: start;
  text-align:left;
}

.yAxis text {
  font-family: $national;
  text-align: center;
  text-anchor: end;
  fill: silver;
  font-weight: bold;
}
    

span{
  font-family: sans-serif
}

details {
  max-width: 38rem;
  margin: 0 auto 2rem auto;
  padding: 0rem 1rem 0rem 2rem;
}

summary {
  font-family: $barlow;
  font-size: 14px;
  font-weight: 600;
  margin-left: -0.75rem;
}

.bubble-notes {
  max-width: $column-width;
  font-family: $national;
  font-size: 14px;
  margin: 0 auto;
  padding: 1rem 1rem 0 1rem;
  line-height: 1.45;
  text-align: left;
}

/* method section */

.inputTitle {
  font-family: $barlow;
  font-size:20px;
  font-weight: 600;
  color: $off-black;
  text-transform: uppercase;
  padding: 0 1rem;
}

.methodDescription {
  max-width: $column-width;
  font-family: $national;
  font-size: 14px;
  margin: 0 auto;
  padding: 1rem 1rem;
  line-height: 1.45;
  text-align: left;
}

.methodSep {
  width: 100%;
  max-width: $column-width;
  border: 1px solid $off-black;
  margin: 4rem auto 1rem auto;
}

.intextLink:hover {
  border-bottom: 1px solid $red;
}

/* Polarity Comparison bubble chart styling */
.polarityCompyAxis text {
  font-family: $barlow;
  text-align: left;
  text-anchor: end;
  fill: $off-black;
  font-size: 14px;
  font-weight: 600;
  text-transform: uppercase;
}

.polarityCompyAxis .domain {
    stroke:none;
}
  
.grid {
  stroke: lightgrey;
  stroke-width: 1px;
  shape-rendering: crispEdges;
}
  
.polarityCompBubbleLeft {
  fill: $off-black;
  stroke: $off-black;
  stroke-width: 2.5px;
  box-sizing: border-box;
}
  
.polarityCompBubbleRight {
  fill: $green;
  stroke: $off-black;
  stroke-width: 2.5px;
  box-sizing: border-box;
}
  
.polarityCompFemText {
  fill: $green;
  font-family: $barlow;
  font-weight: 600;
  font-size: 14px;
  text-anchor: middle;
  transform: translate(0,-40px);
}

.polarityDiffAnnotation {
  fill: $off-black;
  font-family: $barlow;
  font-weight: 600;
  font-size: 14px;
  text-anchor: left;
  transform: translate(20px, -10px);
}
  
.polarityCompAllText {
  fill: $off-black;
  font-family: $barlow;
  font-size: 14px;
  text-anchor: middle;
  transform: translate(0,-20px)
}
  
.polarityCompxAxisLabel {
  font-size: 14px;
  fill: $off-black;
  font-family: $barlow;
}
  
.polarityCompxAxis {
  display:none;
} 

.lollipop {
    width: 100%;
    max-width: 60rem;
    padding: 0 1rem;
    margin: 0 auto;
}

#lollipopChart {
  width: 100%;
}

/* dropdown */

.sectionHeader, .bubbleFilters {
  padding: 0 1rem;
}

.dropdownOptions {
  background-color: $tan;
  border: 1px solid $off-black;
  font-size: 16px;
  font-weight: 600;
  text-transform: uppercase;
  padding: 0.25rem;
  margin: 1rem 0.25rem;
  font-family: $barlow;
  font-weight: 400;
  border-radius: 0.25rem;
  cursor: pointer;
  width: 8rem;
  height: 2rem;
}

.example

.bubbleFilters, .temporalFilters {
  text-align: right;
  background-color: $tan;
  z-index: 1;
  display: flex;
  flex-direction: row;
  width: 100%;
  align-items: center;
  position: sticky;
  top: 0;
  border-bottom: 1px solid $off-black;
  padding: 1rem;
  margin-top: -0.5rem;
}

.bubbleFilters {
  justify-content: center;
}

.temporalFilters {
  flex-direction: column;
  justify-content: space-between;
  margin-top: 2rem;
  border-top: 1px solid #282828;
}

.temporal-wrapper {
  display: flex;
  flex-direction: row;
  justify-content: center;
  align-items: center;
}

.categories {
  display: flex;
  flex-direction: row;
  margin: 0;
  flex-wrap: wrap;
  max-width: 30rem;
}

.categories button {
  font-family: $barlow;
  font-size: 16px;
  font-weight: 600;
  width: 12rem;
  padding: 0.25rem;
  margin: 0.25rem;
  border-radius: 0.25rem;
}

#btn-violence {
  color: $off-black;
  border: 2px solid $red;
}

.active-btn-violence, #btn-violence:hover {
  color: $off-black !important;
  background: $red;
}

#btn-empowerment {
  color: $off-black;
  border: 2px solid $purple;
}

.active-btn-empowerment, #btn-empowerment:hover {
  color: $off-black !important;
  background: $purple;
}

#btn-stereotypes {
  color: $off-black;
  border: 2px solid $pink;
}

.active-btn-stereotypes, #btn-stereotypes:hover {
  color: $off-black !important;
  background: $pink;
}

#btn-race {
  color: $off-black;
  border: 2px solid $green;
}

.active-btn-race, #btn-race:hover {
  color: $off-black !important;
  background: $green;
}

.active-btn-violence, .active-btn-stereotypes, .active-btn-empowerment, .active-btn-race {
  pointer-events: none;
}

.right svg {
  margin: 0 0.25rem 0 0;
  fill: none;
}

.right svg .st1 {
  stroke: $off-black;
  stroke-width: 2;
}

.left, .right {
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: center;
}

.left {
  margin-right: 2rem;
}

.left div {
  display: flex;
  flex-direction: row;  
}

.right {
  margin-bottom: 0rem; 
}

.left p, .right p {
  font-family: $barlow;
  font-size: 14px;
  color: $off-black;
  margin: 0 0.25rem 0 0;
  text-align: right;
}

.scoreChartBar {
  stroke: $off-black;
  stroke-width: 0.5px;
}

.scoreChartTitle {
  margin-bottom: 30px;
  font-weight:500;
  transform: translateX(10px);
  font-size:10px;
  max-width: 10px;
  text-align: center;
  font-family: $national;
  font-style: bold;
  text-transform: uppercase;
  color: $off-black;
}

.scoreChartBarText {
    font-family: $national;
    font-size: 9px;
}

.lollipopTextbutton {
  color: $off-black;
  font-weight: 600;
  font-family: $barlow;
  font-size: 16px;  
  background-color: $tan;  
  text-transform: uppercase;
  white-space: nowrap;
  border-radius: 0.25rem;
  border 2px solid $green;
  height: 1.9rem;
  margin: 0.025rem 0;
  padding: 0.125rem 0.5rem;
}

.lollipopTextbutton-active {
  color: $off-black;
  font-weight: 600;
  font-family: $barlow;
  font-size: 16px;  
  background-color: $green;
  border 2px solid $green;
  text-transform: uppercase;
  white-space: nowrap;
  border-radius: 0.25rem;
  pointer-events: none;
  height: 1.9rem;
  margin: 0.025rem 0;
  padding: 0.125rem 0.5rem;
}

#wordSearch {

  font-family: $barlow;
  font-weight: 600;
  font-size: 16px;
  text-transform: uppercase;
  padding: 0.25rem 0.5rem;
  white-space: nowrap;
  border-radius: 0.25rem;  
  border: 1px solid;
  background-color: $bg;
  }

#inTextViolence {
  color: $off-black;
  border: 2px solid $red;
}
#inTextViolence-none {
  color: $off-black;
  background: $red;
  border: 2px solid $red;
  pointer-events: none;
}
#inTextStereotypes {
  color: $off-black;
  border: 2px solid $pink;
}
#inTextStereotypes-none {
  background: $pink;
  color: $off-black;
  pointer-events: none;
  border: 2px solid $pink;
}
#inTextEmpowerment {
  color: $off-black;
  border: 2px solid $purple;
}
#inTextPeople {
  color: $off-black;
  border: 2px solid $yellow;
}
#inTextRace {
  color: $off-black;
  border: 2px solid $green;
}

#inTextNT {
  color: grey;
  border: 1px solid grey;
}

.lollipopTextbutton:hover {
  background-color: $green;
  color: $off-black;
  font-weight: 600;
}

#inTextViolence:hover, #inTextViolence.hovered {
  background-color: $red;
  color: $off-black;
  font-family: $barlow;
  font-weight: 600;
  font-size: 16px;
  text-transform: uppercase;
  padding: 0.25rem 0.5rem;
  white-space: nowrap;
  border-radius: 0.25rem;
  cursor: pointer;
}

#inTextStereotypes:hover, #inTextStereotypes.hovered {
  background-color: $pink;
  color: $off-black;
  font-family: $barlow;
  font-weight: 600;
  font-size: 16px;
  text-transform: uppercase;
  padding: 0.25rem 0.5rem;
  white-space: nowrap;
  border-radius: 0.25rem;
  cursor: pointer;
}

#inTextBias:hover, #inTextBias.hovered {
  background-color: $green;
  color: $bg;
  font-family: $barlow;
  font-weight: 600;
  font-size: 16px;
  text-transform: uppercase;
  padding: 0.25rem 0.5rem;
  white-space: nowrap;
  border-radius: 0.25rem;
  cursor: pointer;
}

#inTextEmpowerment:hover, #inTextEmpowerment.hovered {
  background-color: $purple;
  color: $off-black;
  font-family: $barlow;
  font-weight: 600;
  font-size: 16px;
  text-transform: uppercase;
  padding: 0.25rem 0.5rem;
  white-space: nowrap;
  border-radius: 0.25rem;
  cursor: pointer;
}

#inTextPeople:hover, #inTextPeople.hovered {
  background-color: $yellow;
  color: $off-black;
  font-family: $barlow;
  font-weight: 600;
  font-size: 16px;
  text-transform: uppercase;
  padding: 0.25rem 0.5rem;
  white-space: nowrap;
  border-radius: 0.25rem;
  cursor: pointer;
}

#inTextRace:hover, #inTextRace.hovered {
  background-color: $green;
  color: $off-black;
  font-family: $barlow;
  font-weight: 600;
  font-size: 16px;
  text-transform: uppercase;
  padding: 0.25rem 0.5rem;
  white-space: nowrap;
  border-radius: 0.25rem;
  cursor: pointer;
}

#inTextNT:hover, #inTextNT.hovered {
  background-color: grey;
  color: $bg;
  font-family: $barlow;
  font-weight: 600;
  font-size: 16px;
  text-transform: uppercase;
  padding: 0.25rem 0.5rem;
  white-space: nowrap;
  border-radius: 0.25rem;
  cursor: pointer;
}

.stackedBars rect {
  cursor: pointer;
  pointer-events: none;
}

.stackedChartTicks-light {
  font-weight: 500;
  text-transform: none;
}

/* STACKED CHART SCROLLY*/
#scrolly-side{
  width: 100%;
  max-width: 60rem;
  margin: 3rem auto;
  padding: 1rem;
}

.scrolly {
  width: 100%;
  position: relative;
}

.scrolly figure {
  position: sticky;
  top: 0;
  padding: 0 1rem;
  max-width: 40rem;
  margin-left: 24rem;
}

article {
  width: 100%;
  max-width: 24rem;
  position: relative;
  margin: 0
}
  
.step {
  min-height: 110vh;
  margin-bottom: 1rem;
  transition: background-color 250ms ease-in-out;
  text-align: justify;
  color: #282828;
  font-family: $barlow;
  font-size: 16px;
  margin-bottom: 0;
  opacity: 0.6;
}

.step.is-active {
  opacity: 1;
}

.step-inner {
  background-color: #FEFAF1;
  padding-bottom: 1rem;
}3
.step:last-of-type {
  margin-bottom: 0;
  min-height:10vh
}

#stickyStackedChart {
  max-width: 380px;
  width: 380px;
  margin: 0;
  height: 110vh;
}

.stackedBarAnnotation {
  fill: $red;
  font-family: $national;
  font-weight: 600;
  font-size: 12px;
  text-anchor:end;
  text-transform: uppercase;
  transform: translate(100px, 5px)
}

.stackedBarTextAnnotation, .stackedBarThemeAnnotation {
  font-family: $barlow;
  font-weight: 600;
  font-size: 16px;
  text-transform: uppercase;
  padding: 0.25rem 0.5rem;
  white-space: nowrap;
  border-radius: 0.25rem;
  cursor: pointer;
  color: $off-black;
  border 1px solid $off-black;
}

.stackedBarThemeHover {
    font-family: $barlow;
    font-weight: 600;
    font-size: 14px;
    text-transform: uppercase;
    fill: $off-black;
}

.hoverRectActive {
  //fill: $off-black !important;
  opacity: 1 !important;
}

#hover-black {
  color: $off-black;
  border 1px solid $off-black;
}

#hover-black-none {
  color: $tan;
  border 1px solid $off-black;
  background: $off-black;
  pointer-events: none;
}

#hover-black:hover {
  color: $bg;
  background: $off-black;
  border 1px solid $off-black;
}

#hover-red {
  color: $red;
  border 1px solid $red;
}

#hover-red-none {
  color: $off-black;
  border 1px solid $red;
  background: $red;
  pointer-events: none;
}

#hover-red:hover {
  color: $bg;
  background: $red;
  border 1px solid $red;
}

#hover-green {
  color: $green;
  border 1px solid $green;
}

#hover-green-none {
  color: $black;
  border 1px solid $pink;
  background: $pink;
  pointer-events: none;
}

#hover-green:hover {
  color: $bg;
  background: $green;
  border 1px solid $green;
}

#hover-pink {
  color: $pink;
  border 1px solid $pink;
}

#hover-pink:hover {
  color: $bg;
  background: $pink;
  border 1px solid $pink;
}

#hover-yellow {
  color: $yellow;
  border 1px solid $yellow;
}

#hover-yellow:hover {
  color: $bg;
  background: $yellow;
  border 1px solid $yellow;
}

.word-legend, .freq-legend {
  font-family: $tiempos;
  font-weight: 600;
}

.word-legend::after {
  display: inline-block;
  content: '';
  width: 3rem;
	height: 0.25rem;
  background-color: #ded5bb;
  margin: 0 0.25rem 0.125rem 0.25rem;
}

.freq-legend::after {
  display: inline-block;
  content: '↕';
  line-height: 1;
  padding: 0.25rem;
  width: 4rem;
	height: 1rem;
  top: 1px;
  position: relative;
  text-align: right;
  background: linear-gradient(to right, #ded5bb 80%, 
                            #FEFAF1 80%);
  margin: 0 0 0 0.5rem;
}

.stackedBars {
  transform: translateX(10px)
}

.stackedChartCountries {
  margin-bottom:20px;
  margin-top:30px;
  font-weight: 600;
  font-family: $barlow;
  transform: translate(10px, 80px);
  max-width: 13px;
  text-align: center;
  fill: #FEFAF1;
  z-index: 10
  
}

.stackedChartTicks {
  font-weight:600;
  font-size:13px;
  font-family: $barlow;
  text-transform: uppercase;
  fill: #282828;
}

.themesChartTicks {
  font-weight:600px;
  font-size:12px;
  font-family: Arial, Helvetica, sans-serif;
  text-transform: uppercase;
  fill: #282828;
}

.stackedChartyTicks, .lineChartyAxis {
  font-weight:600;
  font-size:13px;
  font-family: $barlow;
  text-transform: uppercase;
  fill: #282828;
}

.themesChartyTicks {
  font-weight:600;
  font-size:15px;
  font-family: Arial, Helvetica, sans-serif;
  text-transform: uppercase;
  fill: #282828;
}

.spacer {
  height:55vh
}

#timeSeriesChart {
  padding: 0 1rem;
  max-width: $column-width;
  margin: 2rem auto;
  height: 300px;
}

#chartP, #chartB {
  width: 100%;
  height: 420px;
  margin: 0 auto;
}

#chartP svg, #chartB svg {
  width: 100%;
  height: 100%;
}

#smChart {
  width: 100%;
  height: 14000px;
  padding: 0 1rem;
  margin: 0 auto;
  max-width: 60rem;
  transition: height 0.25s ease-in;
}

.pudding-chart {
  width: 100%;
  height: 100%;
}

.chartLabels {
  padding: 0 1rem;
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  margin: 0 auto 2rem auto;
  width: 100%;
  max-width: 60rem;
}

.women-comp-line {
  fill: none;
  stroke: $green;
  stroke-width: 3;
}

.all-comp-line {
  fill: none;
  stroke: $off-black;
  stroke-width: 2;
}

.barbellLabels {
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  width: 100%;
  max-width: 60rem;
  margin: 0 auto 2rem auto;
  padding: 0 50px 0 160px;
}


.barbellLegend {
  width: 100%;
  padding: 0 3.5rem 0 11.5rem;
  display: flex;
  flex-direction: column;
  margin: 2rem auto 0 auto;
  max-width: 60rem;
}

.leg1 {
  display: flex;
  flex-direction: row;
  position: relative;
  justify-content: space-between;
  width: 16.5rem;
  align-items: flex-end;
  margin-bottom: 0.5rem;
}

.all-hed {
  color: $off-black;
  font-family: $barlow;
  font-size: 14px;
  text-align: center;
  margin: 0 0 0 -1.25rem;
}

.women-hed {
  color: $off-black;
  font-family: $barlow;
  font-weight: 600;
  font-size: 14px;
  text-align: center;
  max-width: 6rem;
  line-height: 1;
  margin: 0;
}

.leg2 {
  display: flex;
  flex-direction: row;
  align-items: center;
}

.smDot {
  border-radius: 50%;
  width: 0.65rem;
  height: 0.65rem;
  background: $off-black;
}

.line {
  border-top: 3px solid $off-black;
  width: 12rem;
}

.lgDot {
  border-radius: 50%;
  width: 1.5rem;
  height: 1.5rem;
  background: $green;
  border: 3px solid $off-black;
}

.temporal-chart-section {
  width: 100%;
}

.bubble-wrapper {
  position: relative;
}




/* Input styling */

.autocomplete__wrapper
    position relative

label 
    display none

.autocomplete__hint,
.autocomplete__input
    -webkit-appearance none
    border 2px solid #0b0c0c
    border-radius 0
    box-sizing border-box
    -moz-box-sizing border-box
    -webkit-box-sizing border-box
    margin-bottom 0
    width 100%

.autocomplete__input
    background-color transparent
    position relative

.autocomplete__hint
    color #b1b4b6
    position absolute

.autocomplete__input--default
    padding 5px

.autocomplete__input--focused
    outline 3px solid $karen-red
    outline-offset 0
    box-shadow inset 0 0 0 2px

.autocomplete__input--show-all-values
    padding 5px 34px 5px 5px
    cursor pointer

.autocomplete__dropdown-arrow-down
    z-index -1
    display inline-block
    position absolute
    right 8px
    width 24px
    height 24px
    top 10px

.autocomplete__menu
    background-color #fff
    border 2px solid #0b0c0c
    border-top 0
    color #0b0c0c
    margin 0
    max-height 342px
    overflow-x hidden
    padding 0
    width 100%
    width calc(100% - 4px)

.autocomplete__menu--visible
    display block

.autocomplete__menu--hidden
    display none

.autocomplete__menu--overlay
    box-shadow rgba(0, 0, 0, .256863) 0 2px 6px
    left 0
    position absolute
    top 100%
    z-index 100

.autocomplete__menu--inline
    position relative

.autocomplete__option
    border-bottom solid #b1b4b6
    border-width 1px 0
    cursor pointer
    display block
    position relative
    & > *
        pointer-events none
    &:first-of-type
        border-top-width 0
    &:last-of-type
        border-bottom-width 0

.autocomplete__option--odd
    background-color #fafafa

.autocomplete__option--focused,
.autocomplete__option:hover
    background-color #1d70b8
    border-color #1d70b8
    color #fff
    outline 0

.autocomplete__option--no-results
    background-color #fafafa
    color #646b6f
    cursor not-allowed

.autocomplete__hint,
.autocomplete__input,
.autocomplete__option
    font-size 16px
    line-height 1.25

.autocomplete__hint,
.autocomplete__option
    padding 5px

@media only screen and (max-width: 960px) {
  .title {
    font-size:120px;
  } 

  .titleArt {
    height: 476px;
  }

  .title span {
    padding: 1rem 2rem 0rem 1rem;
  }
}

@media only screen and (max-width: 800px) {
  #stickyStackedChart {
    margin: 0 auto;
  }
  article {
    margin: 0 auto;
  }

  .scrolly figure {
    margin: 0 auto;
  } 

  .titleArt {
    height: auto;
    flex-direction: column;
    border-bottom: none;
  }

  .titlewords {
    width: 100%;
    border-right: none;
  }

  .title {
    text-align: center;
  }

  .title span:last-of-type {
    border-bottom: 1px solid $off-black;
  }

  .titleCredits {
    padding: 0 1rem;
    width: 100%;
  }

  .subTitleArt {
    max-width: 40rem;
    margin: 0 auto;
    padding: 2rem 0;
    text-align: center;
  }

  .credits {
    max-width: 40rem;
    margin: 0 auto;
    text-align: center;
  }

  .credits a {
    font-weight: 600;
    text-transform: uppercase;
  }

  .bubbleFilters, .temporalFilters {
    flex-direction: column;  
    justify-content: center;
  }

  .categories {
    margin-top: 1rem;
    justify-content: center;
  }

  .temporal-wrapper {
    flex-direction: column;
  }

  .dropdownOptions {
    margin: 0rem 0.25rem;
  }

  #stickyXaxis {
    top: 140px;
  }

  .padded-dropcap {
    font-size: 48px;
  }
}

@media only screen and (max-width: 600px) {
  .title {
    font-size:80px;
  } 

  .sectionHeader, .padded-dropcap {
    font-size: 36px;
  }

  .tooltipTL {
    width: 100%;
    max-width: 25rem;
    margin: 1rem 0;
  }

  .barbellLabels {
    padding: 0 50px;
  }

  .barbellLegend {
    padding: 0 3.5rem 0 3.5rem;
  }

  .tooltip {
    bottom: 0;
    left: 0;
    top: auto;
    width: 100%;
  }

  .lgDot {
    width: 1.25rem;
    height: 1.25rem;
    border: 3px solid $off-black;
  }

  .smDot {
    width: 0.5rem;
    height: 0.5rem;
  }

  #chartP, #chartB {
    height: 500px;
  }

  .chartLabels {
    transform-origin: 100% 0;
    transform: rotate(90deg);
    position: absolute;
    height: 2rem;
    bottom: -2rem;
    right: 0;
    width: 460px;
  }
  .xAxisLabel-left {
    position: absolute;
    left: 0;
    bottom: 0;
  }

  .xAxisLabel-right {
    position: absolute;
    right: 0;
    bottom: 0;
  }
}

@media only screen and (max-width: 450px) {
  .categories button {
    font-size: 14px;
    width: 10rem;
  }

  .dropdownOptions {
    font-size: 14px;
  }
}